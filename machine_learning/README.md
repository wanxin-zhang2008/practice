# Machine Learning

机器学习相关代码与实验将统一上传到此文件夹。

## 建议结构

- `data/`    数据集
- `scripts/` 训练 / 评估脚本
- `output/`  模型、图表等输出结果

## 上传方式

- GitHub 网页：Add file → Upload files
- 命令行：`git add machine_learning/ && git commit && git push`